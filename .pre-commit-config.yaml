repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff
        name: ruff (backend lint; matches CI E/W/F rules)
        files: ^backend/.*\.py$
        args: [check, --select=E,W,F, --ignore=E501]
